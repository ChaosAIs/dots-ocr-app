# 生产环境依赖
# streamlit
gradio
gradio_image_annotation
PyMuPDF
openai
qwen_vl_utils
transformers==4.51.3
huggingface_hub
modelscope
flash-attn==2.8.0.post2
accelerate
python-dotenv
fastapi
uvicorn
python-multipart

# RAG Chatbot dependencies (LangChain 1.0+)
# Upgraded from 0.3.x to 1.0+ for stability and new features
langchain>=1.1.0
langchain-core>=1.1.0
langchain-openai>=1.1.0
langchain-qdrant>=1.1.0
langchain-ollama>=1.0.0
langchain-text-splitters>=1.0.0
langgraph>=1.0.0
# Note: langchain-community is deprecated in 1.0, functionality moved to specific packages
# Note: langchain-classic available for legacy chain compatibility if needed
qdrant-client
watchdog
requests
dateparser  # For date normalization in RAG pipeline

# Database dependencies
psycopg2-binary
asyncpg
sqlalchemy[asyncio]

# Graph database dependencies
neo4j

# Text processing
unidecode  # For transliterating Unicode text to ASCII (workspace folder names)
